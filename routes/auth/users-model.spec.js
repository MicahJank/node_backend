const Users = require('./users-model.js');
const db = require('../../data/dbConfig.js');

describe('users model testing', () => {

    describe('find', () => {
        it('should find and return a list off all the users in the database', () => {
            
        })
    })
})